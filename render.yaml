services:
  - type: web
    name: gprint
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SERVER_HOST
        value: "0.0.0.0"
      - key: SERVER_PORT
        value: "8080"
      # Set these in Render dashboard (secret values):
      # - ORACLE_USER
      # - ORACLE_PASSWORD
      # - ORACLE_TNS_ALIAS
      # - JWT_SECRET
